import { ReactNode } from 'react';

export function CommandPalette({ children, open, onOpenChange }: { 
  children?: ReactNode;
  open?: boolean;
  onOpenChange?: (open: boolean) => void;
}) {
  return null;
}
